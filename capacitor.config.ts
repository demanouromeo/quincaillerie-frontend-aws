import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mvogt.quincaillerie',
  appName: 'Quincaillerie Mvogt',
  webDir: 'dist/frontend-angular/browser'
};

export default config;
